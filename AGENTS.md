# Project instructions

- 默认使用中文维护产品文档与 Issue，代码标识符使用英文。
- 产品定位是“网页投屏 / TabReach”：把用户明确选择的 Edge 标签页画面发送到 HarmonyOS 平板；不得称为
  Windows 扩展屏、虚拟显示器或远程桌面。
- 平板端必须是 HarmonyOS 原生实现，不允许引入 HTML5 接收页、WebView Viewer 或 Android APK
  兼容层。
- v0.1 只支持可信局域网，禁止默认开放到公网；不得为了连通性退化为无边界监听。
- v0.1 不采集或传输音频，声音继续由 Windows/Edge 输出到用户原有耳机。
- 不以具体平板型号或网站名称写业务分支；型号和网站只用于兼容性测试记录。
- Windows 端不得引入内核驱动、虚拟显示驱动、系统服务或管理员安装要求。若未来另立“真扩展屏”
  产品线，必须通过 IddCx 或等价的受支持机制实现，不能用镜像冒充。
- Edge 扩展采用最小权限；未经明确论证不得请求 `<all_urls>`、读取页面内容或注入站点脚本。
- 每次修改局域网或本地 Relay 协议时同步更新 `docs/PROTOCOL.md`。
