# Project instructions

- 默认使用中文维护产品文档与 Issue，代码标识符使用英文。
- 平板端必须是 HarmonyOS 原生实现，不允许引入 HTML5 接收页或 Android APK 兼容层。
- v0.1 只支持可信局域网，禁止默认开放到公网。
- 不以具体平板型号写业务分支；型号仅用于兼容性测试记录。
- Windows 真扩展屏必须通过 IddCx 或等价的受支持虚拟显示机制完成，不能用镜像或远程桌面冒充。
- 每次修改协议时同步更新 `docs/PROTOCOL.md`。
